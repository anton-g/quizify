export enum GameState {
  Created = "CREATED",
  Lobby = "LOBBY",
  Playing = "PLAYING",
  Done = "DONE"
}

export enum GameEvents {
  Host = "HOST",
  Update = "UPDATE",
  Start = "START",
  Join = "JOIN",
  Score = "SCORE"
}
